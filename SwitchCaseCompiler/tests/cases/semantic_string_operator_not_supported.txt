#include <string>
using namespace std;

string first = "A";
string last = "B";
string name;
int state = 1;

switch (state) {
    case 1:
        name = first + last;
        break;
    default:
        name = "UNKNOWN";
        break;
}
