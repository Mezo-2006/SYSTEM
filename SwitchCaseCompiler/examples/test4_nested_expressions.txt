// TEST CASE 4: Nested Expressions
// Tests operator precedence and parentheses

switch (x) {
  case 1: y = (a + b) * (c - d); break;
  case 2: z = x / 2 + 3 * 4; break;
  case 3: result = (x + y) / (z - 1); break;
  default: result = 0; break;
}
